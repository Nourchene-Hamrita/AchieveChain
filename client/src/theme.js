import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "rgb(131,58,180)",
            light: "rgb(131,58,180)"
        },
        secondary: {
            main: 'rgb(131,58,180)',
        },
        otherColor: {
            main: "#999"
        }
    }
})